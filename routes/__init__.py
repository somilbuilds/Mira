# this file makes the routes/ directory a Python package.
# without it, `from routes import entries` in main.py would fail.
# Python needs this file to treat a folder as importable.
# it can be completely empty — its existence is what matters.