/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel handles optimization automatically
  // Keep server actions enabled for form handling
  experimental: {},
}

module.exports = nextConfig
